const HelpButton = () => {
  return (
    <button className="w-full py-3 rounded-lg cursor-pointer transition-all duration-300 items-center justify-center flex bg-blue-800 ">
      <p className="font-bold text-xl text-white">
        Precisa de ajuda ?
      </p>
    </button>
  );
};

export default HelpButton;
